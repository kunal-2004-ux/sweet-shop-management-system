datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Sweet {
  id              String   @id @default(uuid())
  name            String
  category        String
  price           Float
  quantityInStock Int
  createdAt       DateTime @default(now())
}
